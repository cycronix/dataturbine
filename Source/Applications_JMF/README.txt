Applications requiring JMF (Java Media Framework) have been moved to separate folder.  They are not built by default, as they require a separate download of the JMF library, which is getting hard to find for some modern OS versions.  

